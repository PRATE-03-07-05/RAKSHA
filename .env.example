# RAKSHA frontend environment
# Base URL of the FastAPI backend (see backend/.env.example for backend config).
VITE_API_BASE_URL=http://localhost:8000

# Prototype flag — the shipped frontend runs on its embedded demo store
# until an API client is wired to the endpoints documented in backend/README.md.
RAKSHA_DEMO_MODE=true
