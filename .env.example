# RAKSHA frontend environment
# Base URL of the FastAPI backend (see backend/.env.example for backend config).
VITE_API_BASE_URL=http://localhost:8000

# Demo banner flag (must use VITE_ prefix to reach import.meta.env).
VITE_RAKSHA_DEMO_MODE=true
